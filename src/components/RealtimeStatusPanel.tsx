import type { SimulationEngine } from '../simulation/simulationEngine';
export function RealtimeStatusPanel({engine,running}:{engine:SimulationEngine;running:boolean}){const s=engine.state,r=s.result;return <section className="panel"><h2>实时状态</h2><dl><dt>当前算法</dt><dd>{engine.controller.name}</dd><dt>当前帧 / 状态</dt><dd>{s.frame} / {running?'仿真中':'已暂停'}</dd><dt>黑线位置</dt><dd>{r.linePosition===null?'丢线':r.linePosition.toFixed(2)}</dd><dt>当前动作</dt><dd>{r.actionLabel}</dd><dt>目标轮速 L / R</dt><dd>{s.targetLeft.toFixed(1)} / {s.targetRight.toFixed(1)}</dd><dt>实际轮速 L / R</dt><dd>{s.actualLeft.toFixed(1)} / {s.actualRight.toFixed(1)}</dd>{Object.entries(r.debugValues).map(([k,v])=><><dt key={k}>{k}</dt><dd key={k+'v'}>{String(v)}</dd></>)}</dl></section>}
